---
import BaseLayout from "../layouts/BaseLayout.astro"
import BlueprintBanner from "../components/BlueprintBanner"
import { getCollection } from "astro:content"

const pageTitle = "低维展开 - 前端全栈成长之路"
const allPosts = await getCollection("blog")
const recentPosts = allPosts
  .sort((a, b) => new Date(b.data.pubDate).getTime() - new Date(a.data.pubDate).getTime())
  .slice(0, 3) // 最新3篇文章
---

<BaseLayout pageTitle={pageTitle}>
  <!-- Blueprint Banner 区域 -->
  <BlueprintBanner 
    client:load
    title="🚀 低维展开" 
    subtitle="Web前端开发 | 全栈探索 | 技术成长"
  />

  <!-- 最新文章 -->
  <section class="space-y-6 mt-12">
    <div class="flex items-center justify-between">
      <h2 class="text-3xl font-bold text-gray-800">📚 最新文章</h2>
      <a href="/blog/" class="text-blue-600 hover:text-blue-800 font-semibold transition-colors">
        查看全部 →
      </a>
    </div>

    {recentPosts.length > 0 ? (
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        {recentPosts.map((post) => (
          <a
            href={`/posts/${post.id}/`}
            class="group p-6 rounded-xl bg-linear-to-br from-blue-50 to-indigo-50 hover:from-blue-100 hover:to-indigo-100 border border-blue-200 hover:border-blue-400 transition-all duration-300 hover:shadow-lg hover:scale-[1.02]"
          >
            <h3 class="text-lg font-bold text-gray-800 group-hover:text-blue-600 transition-colors line-clamp-2 mb-2">
              {post.data.title}
            </h3>
            {post.data.description && (
              <p class="text-gray-600 text-sm line-clamp-2 mb-4">
                {post.data.description}
              </p>
            )}
            <div class="flex items-center justify-between pt-4 border-t border-blue-200">
              <div class="flex gap-2 flex-wrap">
                {post.data.tags?.slice(0, 2).map((tag) => (
                  <span class="text-xs bg-blue-200 text-blue-700 px-2 py-1 rounded-full font-medium">
                    {tag}
                  </span>
                ))}
              </div>
              <span class="text-xs text-gray-500">
                {post.data.pubDate?.toLocaleDateString('zh-CN')}
              </span>
            </div>
          </a>
        ))}
      </div>
    ) : (
      <div class="text-center p-12 bg-gray-50 rounded-xl">
        <p class="text-gray-500">暂无文章</p>
      </div>
    )}
  </section>

  <!-- 开源致谢 -->
  <section class="mt-12 space-y-6">
    <h2 class="text-3xl font-bold text-gray-800">🙏 致谢</h2>
    <p class="text-gray-600 text-lg">感谢以下优秀的开源项目和服务，让这个网站的建设成为可能</p>
    
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
      <!-- Astro -->
      <div class="p-4 rounded-lg bg-linear-to-br from-slate-50 to-slate-100 border border-slate-200 hover:shadow-md transition-all">
        <div class="flex items-start gap-3">
          <span class="text-3xl">⚡</span>
          <div class="flex-1">
            <h3 class="font-bold text-gray-800">Astro</h3>
            <p class="text-sm text-gray-600 mt-1">现代化的静态网站生成器，提供优异的性能和开发体验</p>
            <a href="https://astro.build" target="_blank" class="text-xs text-blue-600 hover:underline mt-2 inline-block">
              访问官网 →
            </a>
          </div>
        </div>
      </div>

      <!-- React -->
      <div class="p-4 rounded-lg bg-linear-to-br from-blue-50 to-cyan-100 border border-blue-200 hover:shadow-md transition-all">
        <div class="flex items-start gap-3">
          <span class="text-3xl">⚛️</span>
          <div class="flex-1">
            <h3 class="font-bold text-gray-800">React</h3>
            <p class="text-sm text-gray-600 mt-1">用于构建用户界面的 JavaScript 库，驱动交互式组件</p>
            <a href="https://react.dev" target="_blank" class="text-xs text-blue-600 hover:underline mt-2 inline-block">
              访问官网 →
            </a>
          </div>
        </div>
      </div>

      <!-- Tailwind CSS -->
      <div class="p-4 rounded-lg bg-linear-to-br from-cyan-50 to-blue-100 border border-cyan-200 hover:shadow-md transition-all">
        <div class="flex items-start gap-3">
          <span class="text-3xl">🎨</span>
          <div class="flex-1">
            <h3 class="font-bold text-gray-800">Tailwind CSS</h3>
            <p class="text-sm text-gray-600 mt-1">功能优先的 CSS 框架，快速构建现代化的设计</p>
            <a href="https://tailwindcss.com" target="_blank" class="text-xs text-blue-600 hover:underline mt-2 inline-block">
              访问官网 →
            </a>
          </div>
        </div>
      </div>

      <!-- Daisy UI -->
      <div class="p-4 rounded-lg bg-linear-to-br from-purple-50 to-pink-100 border border-purple-200 hover:shadow-md transition-all">
        <div class="flex items-start gap-3">
          <span class="text-3xl">🌼</span>
          <div class="flex-1">
            <h3 class="font-bold text-gray-800">Daisy UI</h3>
            <p class="text-sm text-gray-600 mt-1">基于 Tailwind CSS 的组件库，提供美观的 UI 组件</p>
            <a href="https://daisyui.com" target="_blank" class="text-xs text-blue-600 hover:underline mt-2 inline-block">
              访问官网 →
            </a>
          </div>
        </div>
      </div>

      <!-- Monaco Editor -->
      <div class="p-4 rounded-lg bg-linear-to-br from-indigo-50 to-purple-100 border border-indigo-200 hover:shadow-md transition-all">
        <div class="flex items-start gap-3">
          <span class="text-3xl">✏️</span>
          <div class="flex-1">
            <h3 class="font-bold text-gray-800">Monaco Editor</h3>
            <p class="text-sm text-gray-600 mt-1">VS Code 的代码编辑器，为代码示例提供实时交互</p>
            <a href="https://microsoft.github.io/monaco-editor/" target="_blank" class="text-xs text-blue-600 hover:underline mt-2 inline-block">
              访问官网 →
            </a>
          </div>
        </div>
      </div>

      <!-- GitHub Pages -->
      <div class="p-4 rounded-lg bg-linear-to-br from-gray-50 to-gray-100 border border-gray-300 hover:shadow-md transition-all">
        <div class="flex items-start gap-3">
          <span class="text-3xl">🚀</span>
          <div class="flex-1">
            <h3 class="font-bold text-gray-800">GitHub Pages</h3>
            <p class="text-sm text-gray-600 mt-1">免费的静态网站托管服务，快速可靠的全球 CDN</p>
            <a href="https://pages.github.com" target="_blank" class="text-xs text-blue-600 hover:underline mt-2 inline-block">
              访问官网 →
            </a>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- 统计信息 -->
  <section class="mt-12 space-y-6">
    <h2 class="text-3xl font-bold text-gray-800">📊 网站数据</h2>
    
    <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
      <div class="p-6 rounded-xl bg-linear-to-br from-blue-50 to-blue-100 border border-blue-200 hover:shadow-lg transition-all">
        <div class="text-4xl font-bold text-blue-600">{allPosts.length}</div>
        <div class="text-gray-600 text-sm mt-2 font-medium">篇原创文章</div>
      </div>
      <div class="p-6 rounded-xl bg-linear-to-br from-purple-50 to-purple-100 border border-purple-200 hover:shadow-lg transition-all">
        <div class="text-4xl font-bold text-purple-600">6+</div>
        <div class="text-gray-600 text-sm mt-2 font-medium">年技术经验</div>
      </div>
      <div class="p-6 rounded-xl bg-linear-to-br from-indigo-50 to-indigo-100 border border-indigo-200 hover:shadow-lg transition-all">
        <div class="text-4xl font-bold text-indigo-600">100%</div>
        <div class="text-gray-600 text-sm mt-2 font-medium">开源免费</div>
      </div>
      <div class="p-6 rounded-xl bg-linear-to-br from-pink-50 to-pink-100 border border-pink-200 hover:shadow-lg transition-all">
        <div class="text-4xl font-bold text-pink-600">♾️</div>
        <div class="text-gray-600 text-sm mt-2 font-medium">持续更新中</div>
      </div>
    </div>
  </section>
</BaseLayout>
