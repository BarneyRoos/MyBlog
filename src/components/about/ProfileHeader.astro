---
interface Props {
  profile: {
    name: string;
    title: string;
    location: string;
    experience: string;
    education: string;
    email: string;
    phone: string;
    blog: string;
  };
}

const { profile } = Astro.props;
---

<div class="relative">
  <div class="absolute inset-0 bg-linear-to-r from-blue-100 to-indigo-100 rounded-2xl opacity-50"></div>
  <div class="relative p-6 rounded-2xl border border-blue-200">
    <h1 class="text-5xl font-bold mb-2 mt-0">
      {profile.name}
    </h1>
    <div class="flex flex-col md:flex-row items-start md:items-center justify-between gap-6">
      <!-- 左侧：基本信息 -->
      <div>
        
        <p class="text-2xl text-gray-700 font-semibold mb-3">{profile.title}</p>
        <div class="space-y-2 text-gray-600">
          <p>📍 {profile.location} | 🎓 {profile.education}</p>
          <p>💼 {profile.experience}</p>
        </div>
      </div>

      <!-- 右侧：联系信息 -->
      <div class="space-y-3 text-sm">
        <a href={`mailto:${profile.email}`} class="flex items-center gap-2 text-blue-600 hover:text-blue-800 transition-colors">
          <span>📧</span>
          <span>{profile.email}</span>
        </a>
        <div class="flex items-center gap-2 text-gray-700">
          <span>📱</span>
          <span id="phone-mask" style="cursor:pointer;user-select:none;">
            <svg id="phone-hide-icon" xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-eye-off-icon lucide-eye-off"><path d="M10.733 5.076a10.744 10.744 0 0 1 11.205 6.575 1 1 0 0 1 0 .696 10.747 10.747 0 0 1-1.444 2.49"/><path d="M14.084 14.158a3 3 0 0 1-4.242-4.242"/><path d="M17.479 17.499a10.75 10.75 0 0 1-15.417-5.151 1 1 0 0 1 0-.696 10.75 10.75 0 0 1 4.446-5.143"/><path d="m2 2 20 20"/></svg>
            <span id="phone-value" style="display:none;">{profile.phone}</span>
          </span>
        </div>
        <a href={profile.blog} target="_blank" class="flex items-center gap-2 text-blue-600 hover:text-blue-800 transition-colors">
          <span>🌐</span>
          <span>个人博客</span>
        </a>
      </div>
    </div>
  </div>
</div>

<script is:inline>
  const mask = document.getElementById('phone-mask');
  const phone = document.getElementById('phone-value');
  const icon = document.getElementById('phone-hide-icon');
  if(mask && phone && icon) {
    mask.addEventListener('click', () => {
      if(phone.style.display === 'none') {
        phone.style.display = 'inline';
        icon.style.display = 'none';
      } else {
        phone.style.display = 'none';
        icon.style.display = 'inline';
      }
    });
  }
</script>
