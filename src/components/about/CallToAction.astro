---
interface Props {
  email: string;
  phone: string;
}

const { email, phone } = Astro.props;
---

<div class="text-center p-8 rounded-xl bg-linear-to-r from-blue-100 via-purple-100 to-indigo-100 border border-blue-200">
  <p class="text-gray-700 mb-4">
    感兴趣的话，欢迎通过下方联系方式与我沟通😊
  </p>
  <div class="flex gap-4 justify-center flex-wrap">
    <a href={`mailto:${email}`} class="px-6 py-3 bg-blue-600 text-white rounded-lg font-semibold hover:bg-blue-700 transition-colors">
      📧 发送邮件
    </a>
    <a href={`tel:${phone}`} class="px-6 py-3 bg-green-600 text-white rounded-lg font-semibold hover:bg-green-700 transition-colors">
      📱 {phone}
    </a>
  </div>
</div>
