---
interface Props {
  skills: {
    languages: string[];
    frameworks: string[];
    uiLibs: string[];
    stateManagement: string[];
    other: string[];
  };
}

const { skills } = Astro.props;

const skillCategories = [
  {
    icon: "🔤",
    title: "编程语言",
    items: skills.languages,
    bgGradient: "from-blue-50 to-blue-100",
    borderColor: "border-blue-200",
    tagBg: "bg-blue-200",
    tagText: "text-blue-800"
  },
  {
    icon: "⚛️",
    title: "框架",
    items: skills.frameworks,
    bgGradient: "from-purple-50 to-purple-100",
    borderColor: "border-purple-200",
    tagBg: "bg-purple-200",
    tagText: "text-purple-800"
  },
  {
    icon: "🎨",
    title: "UI库",
    items: skills.uiLibs,
    bgGradient: "from-indigo-50 to-indigo-100",
    borderColor: "border-indigo-200",
    tagBg: "bg-indigo-200",
    tagText: "text-indigo-800"
  },
  {
    icon: "⚙️",
    title: "其他技能",
    items: [...skills.stateManagement, ...skills.other],
    bgGradient: "from-pink-50 to-pink-100",
    borderColor: "border-pink-200",
    tagBg: "bg-pink-200",
    tagText: "text-pink-800"
  }
];
---

<div class="space-y-6">
  <h2 class="text-3xl font-bold text-gray-800">技能清单</h2>
  
  <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
    {skillCategories.map((category) => (
      <div class={`p-6 bg-linear-to-br ${category.bgGradient} rounded-xl border ${category.borderColor}`}>
        <h3 class="font-bold text-lg mb-3">
          <span set:text={`${category.icon} ${category.title}`}></span>
        </h3>
        <div class="flex flex-wrap gap-2">
          {category.items.map((skill: string) => (
            <span class={`px-3 py-1 ${category.tagBg} ${category.tagText} rounded-full text-sm font-medium`}>
              {skill}
            </span>
          ))}
        </div>
      </div>
    ))}
  </div>
</div>
