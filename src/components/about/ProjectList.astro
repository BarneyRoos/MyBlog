---
interface Project {
  name: string;
  date: string;
  description: string;
  techStack: string[];
  highlights: string[];
}

interface Props {
  projects: Project[];
}

const { projects } = Astro.props;
---

<div class="space-y-6">
  <h2 class="text-3xl font-bold text-gray-800">代表项目</h2>
  
  <div class="space-y-6">
    {projects.map((project) => (
      <div class="group p-6 rounded-xl border border-blue-200 bg-linear-to-br from-blue-50 to-indigo-50 hover:shadow-lg transition-all duration-300">
        <!-- 项目标题和时间 -->
        <div class="flex flex-col md:flex-row md:items-center md:justify-between gap-2 mb-3">
          <h3 class="text-2xl font-bold text-gray-800 group-hover:text-blue-600 transition-colors">
            {project.name}
          </h3>
          <span class="text-sm font-semibold text-gray-500 whitespace-nowrap">📅 {project.date}</span>
        </div>

        <!-- 项目描述 -->
        <p class="text-gray-700 mb-4 leading-relaxed">
          {project.description}
        </p>

        <!-- 技术栈 -->
        <div class="mb-4">
          <div class="flex flex-wrap gap-2">
            {project.techStack.map((tech) => (
              <span class="px-2 py-1 bg-blue-200 text-blue-800 text-xs rounded font-medium">
                {tech}
              </span>
            ))}
          </div>
        </div>

        <!-- 项目亮点 -->
        <div class="bg-white/60 rounded-lg p-4">
          <p class="font-semibold text-gray-800 mb-2">✨ 项目亮点：</p>
          <ul class="space-y-2">
            {project.highlights.map((highlight) => (
              <li class="text-sm text-gray-700 flex gap-2">
                <span class="text-blue-600 font-bold">•</span>
                <span>{highlight}</span>
              </li>
            ))}
          </ul>
        </div>
      </div>
    ))}
  </div>
</div>
